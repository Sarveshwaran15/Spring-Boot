Spring Boot JDBC is used to interact with relational databases without needing an ORM (like Hibernate). 
It follows a layered architecture, including Controller, Service, Repository (DAO), and Model layers.

